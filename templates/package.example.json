{
  "name": "my-mcp-servers",
  "version": "1.0.0",
  "description": "Self-hosted MCP servers deployment",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "build": "bunx tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@llmbase/mcp-web-fetch": "^1.0.0",
    "@llmbase/mcp-core": "^1.0.0",
    "@llmbase/mcp-shared": "^1.0.0",
    "hono": "^4.6.11",
    "@cloudflare/workers-types": "^4.20241218.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}